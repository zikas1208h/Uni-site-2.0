# Server Configuration
PORT=5000

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/uni-portal

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_change_this_in_production_use_long_random_string

# Environment
NODE_ENV=development

# ── Email / OTP ─────────────────────────────────────────────────────────────
# Used to send verification codes to staff during first-login credential setup
# Gmail: create an App Password at https://myaccount.google.com/apppasswords
SMTP_SERVICE=gmail
SMTP_USER=zikas1208h@gmail.com
SMTP_PASS=nlsm lios szpf tqau

